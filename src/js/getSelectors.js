export const form = document.querySelector('form');
export const button = document.querySelector('button');
export const text = document.querySelector('input');
export const list = document.getElementById('list');
